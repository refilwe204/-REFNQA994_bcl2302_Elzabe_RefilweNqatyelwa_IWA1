// Define a variable `value` and assign it the string value "3"
const value = "3";

// Use the `parseInt()` function to convert `value` to a numerical value, and add it to the numerical value 4.
// Then use `parseInt()` again to convert `value` back to a numerical value, and add it to the result of the previous addition.
// Log the result of this addition to the console.
console.log(parseInt(value) + 4 + parseInt(value));